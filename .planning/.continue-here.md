---
context: default
todo: session-2026-07-22
todo_previous: session-2026-07-20-evening
status: completed
last_updated: 2026-07-22T12:00:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Sharp file I/O on Windows — direct `sharp(input).toFile(output)` with the same path, or `fs.copyFile` from a `.tmp` file immediately after `sharp.toFile()`, can produce `UNKNOWN` / `EPERM` errors. Always read the source into a buffer first (`fs.readFileSync`), then `sharp(buffer).webp(...).toBuffer()`, then `fs.writeFileSync`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Sharp same-path write | Attempting to overwrite a WebP file directly with `sharp(file).toFile(file)` fails with "Cannot use same file for input and output". | blocking | Always use buffer-based pipeline for in-place optimization. |
| Windows temp-file rename race | `sharp.toFile(tmpFile)` may leave the file handle open long enough for `fs.renameSync` / `fs.copyFileSync` to fail with `EPERM`/`UNKNOWN`. | blocking | Read input to buffer, write output buffer to final path. |
| Forgetting leftover `.tmp` files | Temporary files created during failed rename attempts can be accidentally committed. | advisory | After any temp-file attempt, run `find public/images -name '*.tmp' -delete` and verify `git status`. |
| Including gallery in every deploy | `public/images/gallery` is ~36 MB and rarely changes; including it in `dist.zip` bloats deploys unnecessarily. | advisory | Build `dist.zip` from `dist/` excluding `images/` unless gallery files actually changed. |
| Wrapping deploy archives in a folder | If `dist.zip` contains a top-level `dist/` folder, extraction on the server creates `dist/dist/` and files are NOT replaced — they just sit in the subfolder. | blocking | Archive must be FLAT: `index.html`, `assets/`, `api/` at the zip root. Use `build-and-zip.sh`; verify with `python -c "import zipfile; print(zipfile.ZipFile('dist.zip').namelist()[:5])"`. |

<current_state>

- Session 2026-07-22 saved. Repo is private; all commits pushed through `2b9edef`.
- **Perf**: PSI mobile 75→~85 (local 88). Hero h1/img paint instantly from prerender (no entrance animation), skeleton min-heights per measured section heights (CLS 0.44→0), fonts.css inlined into index.html + Onest cyrillic preload (font-swap CLS killed), hero image responsive 700/1400w (103→27KB), slider q70. Real-browser CLS=0 verified via PerformanceObserver.
- **Mobile UX**: heading line breaks (Benefits, PartnershipHero), testimonial swipe+autoplay 5s both sliders (pause on hover), single BackToTop above bottom bar (hides while contact menu open via `sunlife:contact-menu` event), contact menu toggle bug fixed (menuRef scope — ✕ now closes), header py-2, bottom bar pb-0.5rem.
- **Security**: code-track done (rate-limit flock, JSON-LD escape, session TTL 8h, login lockout, users.json removed from repo, deploy key from config). Server-side pending: see remaining_work.
- **Form delivery**: Telegram works via HTTP proxy; email channel down until SMTP_PASS set in server `api/config.local.php`.
- **Hosting sweep**: old /dist.zip (3.2MB) found → deleted by user. `content/*.backup.json` still public (low).
- `dist.zip` (~1.9 MB, FLAT) fresh; deploy via deploy.php with DEPLOY_KEY from server config.local.php.

</current_state>

<completed_work>

- Full perf program: Lighthouse mobile 25→54 local, **live 23→83**; a11y 86→100 (live). GSAP/framer-motion off critical path, codeSplitting.groups, cookie banner delayed past load, image dimensions, contrast, targets, landmarks.
- Code review (typescript-react-reviewer): CRITICAL swapped name/phone handlers in both forms fixed + 21 HIGH items + dead code removal.
- todo-012 done: axe-core 0 violations on 5 pages + keyboard nav verified (audit scripts in `scripts/`).
- todo-014 done: auto-dismissing `ThankYouNote` in both forms, 404 in site layout.
- todo-009 mostly done; LCP element is now hero h1.
- Proxy bug fixes: curl_multi handle map via `spl_object_id`, accept TLS-intercepting proxy certs.

</completed_work>

<remaining_work>

1. **Manual deploy** — upload `dist.zip` + `https://sunlife-photo.ru/deploy.php?key=<DEPLOY_KEY>` (key lives in server `api/config.local.php`), then Ctrl+F5 and verify (hero speed, menu toggle closes, back-to-top hides on menu open).
2. **SMTP_PASS** — mail.ru app password into server `api/config.local.php`; then test form → email arrives.
3. **Apache static option** — enable «Обрабатывать статику через Apache» in hostiman panel (Sites → domain settings): enables 1-year cache headers and closes public *.backup.json. Fallback: support ticket with nginx deny rules.
4. **BOT_TOKEN /revoke** — token was exposed in chat; reissue at @BotFather.
5. Backlog: todo-011 (privacy actualization), todo-005 (Yandex Metrica — needs counter ID), font subsetting for ₽ (~-50KB critical path), todo-013 (CI/CD), todo-004 (Vercel).

</remaining_work>

<decisions_made>

- `dist.zip` excludes `images/gallery` because those files are already on the server and unchanged except for optimization; the deploy script (`deploy.php`) only overwrites paths present in the archive.
- Gallery WebP files were resized to 1600 px on the long edge at quality 75. This balances file size and visual quality.
- Original JPG/PNG files were deleted because every production reference uses `.webp` equivalents.
- Schema.org JSON-LD is rendered by `SchemaOrg` component based on `location.pathname`; FAQPage and Service schemas appear only on the home page; BreadcrumbList is page-specific.

</decisions_made>

<blockers>

- **Credential access**: No FTP/SSH password is stored in this environment; the existing `~/.ssh/id_rsa` was rejected by the hostiman server. Workaround: user performs the FTP upload manually.

</blockers>

## Required Reading (in order)

1. `PLAN.md` / `KIMI.md` / `DESIGN.md` — project goals and stack.
2. `DEPLOY-GUIDE.md` — manual deployment procedure for hostiman.ru.
3. `.planning/todos/completed/todo-010-ux-polish.md` — completed UX scope.
4. `.planning/todos/completed/todo-008-schema-org.md` — completed Schema.org scope.
5. `.planning/todos/pending/` — remaining work.

## Infrastructure State

- **Local build**: clean, no TS/lint errors; prerender 8 routes.
- **dist.zip**: ~1.9 MB flat, located at `f:\VIBECODING\SUNLIFE\sunlife\dist.zip`.
- **Git**: `main` is clean, all changes pushed (`ramzesk1n/sunlife`).
- **Server**: `sunlife-photo.ru`; `api/config.local.php` configured (TG works), SMTP pending.
- **Gotcha**: prerender fails intermittently when zombie chrome.exe processes linger — `taskkill //F //IM chrome.exe` fixes it.

## Pre-Execution Critique Required

- None.

<context>

Paused after a long productive session: perf/a11y program, code review, form delivery hardening, content edits, map updates. Everything is committed and pushed. Next actions are user-side (deploy, SMTP_PASS, token revoke) then backlog todos.

</context>

<next_action>

Ask user: deployed? SMTP_PASS set? If both done — verify form end-to-end (TG + email) and live Lighthouse. Then pick next todo: todo-011 (privacy) or todo-005 (analytics, needs Metrica ID).

</next_action>

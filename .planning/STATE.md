# САН ЛАЙФ - Состояние проекта

## Активная сессия: 2026-07-14

### Что сделано сегодня
1. ✅ **Исправлена загрузка фото в Примеры работ** — `savePartnershipExamples()` теперь обновляет `title` in-place, не пересоздаёт массив
2. ✅ **Исключён `dist/content/` из билда** — Vite плагин удаляет `dist/content/` после сборки, чтобы не перезаписывать серверные данные
3. ✅ **`PartnershipExamples` на `fetch()`** — компонент теперь загружает `partnership.json` с сервера через fetch, а не build-time импорт
4. ✅ **Placeholder создан** — `public/images/placeholder-1.webp` (400×400, #f5f0e8)

### Текущая проблема
- ⚠️ **Фото в Примеры работ не отображаются** — `partnership.json` на сервере был перезаписан пустым при предыдущем деплое. Фото физически загружены в `/images/cms/`, но пути потеряны из JSON.
- **Решение**: Перезагрузить фото через админку (код теперь исправлен, данные не будут теряться)

### Открытые задачи (todo)
#### Критичные (до запуска)
- [ ] todo-001: Bundle size: vendor 71kB gzip → оптимизировать
- [ ] todo-003: Настроить отправку форм (реальный бэкенд) — **ЧАСТИЧНО**: Telegram Bot backend готов, ContactForm отправляет реально
- [ ] todo-004: Деплой на Vercel + кастомный домен
- [ ] todo-006: Заменить placeholder-фото проектов на реальные — **ЧАСТИЧНО**: Команда загружена, Примеры работ загружены в админку но потеряны из JSON
- [ ] todo-007: Защита форм от спама (honeypot + rate limit готовы, reCAPTCHA не начат)

#### Важные (рекомендуется)
- [ ] todo-005: Яндекс.Метрика / Google Analytics
- [ ] todo-008: Schema.org JSON-LD разметка
- [ ] todo-009: Core Web Vitals оптимизация
- [ ] todo-010: UX-полировка (toast, skeleton, back-to-top)

#### Средние (после запуска)
- [ ] todo-011: Актуализация политики конфиденциальности
- [ ] todo-012: Доступность (a11y проверка)
- [ ] todo-014: Страницы 404 и благодарности

#### Низкие (после стабилизации)
- [ ] todo-013: CI/CD, мониторинг, автодеплой
- [ ] PWA, мультиязычность

### Технический долг
- Tailwind v4 sourcemap warning (некритично)
- PHP backend работает только на hostiman (не на Vercel)
- `partnership.json` на сервере пустой — нужно перезагрузить фото через админку

### Следующий шаг
Приоритет: Перезагрузить фото в админке → Примеры работ (роддомы), затем todo-004 (Vercel деплой)
